# GitHub Markdown 数学公式书写规范

本项目的 Markdown 文档托管在 GitHub 上，所有数学公式必须兼容 GitHub 的渲染引擎。

## 核心原则

GitHub 渲染 Markdown 时，**先做 Markdown 语法解析，再做 LaTeX 公式渲染**。这意味着内联公式 `$...$` 中的某些 LaTeX 语法会被 Markdown 解析器误判（尤其是 `_` 下划线和 `{}` 花括号）。

## 内联公式规则

### 规则 1：所有内联公式统一使用 `` $`...`$ `` 语法

**不要使用普通的 `$...$`，全部使用 `` $`...`$ ``（dollar-backtick）语法。**

原因：GitHub 会先做 Markdown 解析再做 LaTeX 渲染，普通 `$...$` 中的下划线、花括号、反斜杠等会被 Markdown 解析器误判。更重要的是，**同一行中如果混用 `$...$` 和 `` $`...`$ ``，普通 `$...$` 的公式会渲染失败**。为避免踩坑，统一使用安全语法。

```markdown
<!-- ❌ 错误：使用普通 $...$ -->
$n$ 阶方阵 $A = (a_{ij})$
$\sigma$ 是一个排列，$\sigma(i)$ 表示第 $i$ 行

<!-- ❌ 错误：混用两种语法（普通的会失效） -->
$\sigma$ 限制在剩余位置上，诱导出 $`S_{n-1}`$ 中的一个排列 $\tau$

<!-- ✅ 正确：全部使用 $`...`$ -->
$`n`$ 阶方阵 $`A = (a_{ij})`$
$`\sigma`$ 是一个排列，$`\sigma(i)`$ 表示第 $`i`$ 行
$`\sigma`$ 限制在剩余位置上，诱导出 $`S_{n-1}`$ 中的一个排列 $`\tau`$
```

### 规则 2：绝对不能混用两种内联语法

**同一行中绝对不能同时出现 `$...$` 和 `` $`...`$ ``**。一旦混用，GitHub 会把普通 `$...$` 的公式当作纯文本输出。

这是最容易犯的错误，也是统一使用 `` $`...`$ `` 的根本原因。

### 规则 3：长公式和复杂集合表达式，改用块级公式

即使用了 `` $`...`$ ``，如果一行文字中内联公式过多或过长（如含有 `\setminus`、`\cup` 的集合表达式），GitHub 仍可能渲染异常。此时应将复杂公式**提取为块级公式 `$$...$$`**，用文字简化行内的描述。

```markdown
<!-- ❌ 一行中塞了多个复杂内联公式，GitHub 容易解析失败 -->
它在剩余行 $`\lbrace 1, \dots, n \rbrace \setminus \lbrace i \rbrace`$ 上给出了
剩余列 $`\lbrace 1, \dots, n \rbrace \setminus \lbrace j \rbrace`$ 的一个双射

<!-- ✅ 行内用文字简述，复杂公式提成块级 -->
它在删去第 $i$ 行后的剩余行上，给出了删去第 $j$ 列后的剩余列的一个双射。

设剩余行指标和剩余列指标分别为（按自然序排列）：

$$
r_1 < r_2 < \cdots < r_{n-1} \quad \text{（即} \ \lbrace 1, \dots, n \rbrace \setminus \lbrace i \rbrace \text{）}
$$
```

**经验法则：一行文字中不要超过 2 个 `` $`...`$ `` 内联公式，超过时拆分段落或改用块级公式。**

## 块级公式规则

### 规则 4：块级公式无需特殊处理

GitHub 对 `$$...$$` 块级公式有独立的解析通道，不受 Markdown 语法干扰，可以正常使用所有 LaTeX 语法。

```markdown
$$
A_{ij} = (-1)^{i+j} M_{ij}
$$
```

### 规则 5：`$$` 必须独占一行

```markdown
<!-- ❌ 错误 -->
$$A_{ij} = (-1)^{i+j} M_{ij}$$

<!-- ✅ 正确 -->
$$
A_{ij} = (-1)^{i+j} M_{ij}
$$
```

### 规则 7：块级公式中不要混用 `\text{}` 和数学命令

GitHub 对 `\text{}` 结束后紧跟数学命令（如 `\lbrace`、`\setminus`）的解析不稳定，容易把数学符号当纯文本输出。

**中文说明应放在公式外面，公式内只写纯数学符号。**

```markdown
<!-- ❌ 错误：\text 和数学命令混用，GitHub 渲染异常 -->
$$
r_1 < r_2 < \cdots < r_{n-1} \quad \text{（即} \ \lbrace 1, \dots, n \rbrace \setminus \lbrace i \rbrace \text{）}
$$

<!-- ✅ 正确：文字在公式外面，公式内只有数学符号 -->
记删去第 $i$ 行后的剩余行指标为：

$$
\lbrace 1, \dots, n \rbrace \setminus \lbrace i \rbrace = \lbrace r_1, r_2, \dots, r_{n-1} \rbrace, \quad r_1 < r_2 < \cdots < r_{n-1}
$$
```

### 规则 6：`$$` 前后要有空行

```markdown
<!-- ❌ 错误：紧贴上文 -->
因此：
$$
\det(A) = \sum_{j=1}^{n} a_{ij} A_{ij}
$$

<!-- ✅ 正确：前后空行 -->
因此：

$$
\det(A) = \sum_{j=1}^{n} a_{ij} A_{ij}
$$
```

## 不兼容的 LaTeX 命令

以下命令在 GitHub 上**不受支持**，必须替换：

| ❌ 不支持 | ✅ 替代方案 | 说明 |
|-----------|-----------|------|
| `\operatorname{sgn}` | `\text{sgn}` 或 `\mathrm{sgn}` | GitHub 不支持 `\operatorname` |
| `\substack{a \\ b}` | 用逗号平铺：`a, \ b` | GitHub 不支持 `\substack` |
| `\boxed{...}` | 去掉，或改用文字强调 | GitHub 不支持 `\boxed` |
| `\mathbb{R}` | 有时不支持，用 `\R` 测试 | 不稳定 |
| `\bm{x}` | `\mathbf{x}` 或 `\boldsymbol{x}` | GitHub 不支持 `\bm` |
| `\xrightarrow{text}` | `\overset{text}{\rightarrow}` | GitHub 不支持 `\xrightarrow` |
| `\tag{1}` | 不使用公式编号 | GitHub 不支持 `\tag` |

## 集合花括号的写法

内联公式中使用 `\lbrace` 和 `\rbrace` 代替 `\{` 和 `\}`，因为后者的花括号可能被 Markdown 解析器吃掉。

**同时，含有 `\lbrace`/`\rbrace` 的内联公式也属于复杂公式，必须用 `` $`...`$ `` 包裹**：

```markdown
<!-- ❌ 错误：普通 $...$ 中 \lbrace/\rbrace 和 \setminus 仍可能被解析异常 -->
$\lbrace 1, 2, 3 \rbrace$
$\lbrace 1, \dots, n \rbrace \setminus \lbrace i \rbrace$

<!-- ✅ 正确：用 $`...`$ 包裹 -->
$`\lbrace 1, 2, 3 \rbrace`$
$`\lbrace 1, \dots, n \rbrace \setminus \lbrace i \rbrace`$

<!-- ✅ 块级公式中两种都可以，无需特殊处理 -->
$$
S = \{1, 2, \dots, n\}
$$
```

## 完整示例

以下是一个符合规范的段落示例：

```markdown
对于 $`n`$ 阶方阵 $`A = (a_{ij})`$，其行列式定义为：

$$
\det(A) = \sum_{\sigma \in S_n} \text{sgn}(\sigma) \prod_{i=1}^{n} a_{i, \sigma(i)}
$$

其中 $`S_n`$ 是集合 $`\lbrace 1, 2, \dots, n \rbrace`$ 上所有排列的集合。
每个排列 $`\sigma`$ 都有一个确定的值 $`\sigma(i) = j`$，其中 $`j \in \lbrace 1, 2, \dots, n \rbrace`$。
余子式 $`M_{ij}`$ 是删去第 $`i`$ 行第 $`j`$ 列后的 $`(n-1)`$ 阶行列式。
```

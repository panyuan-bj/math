# GitHub Markdown 数学公式书写规范

本项目的 Markdown 文档托管在 GitHub 上，所有数学公式必须兼容 GitHub 的渲染引擎。

## 核心原则

GitHub 渲染 Markdown 时，**先做 Markdown 语法解析，再做 LaTeX 公式渲染**。这意味着内联公式 `$...$` 中的某些 LaTeX 语法会被 Markdown 解析器误判（尤其是 `_` 下划线和 `{}` 花括号）。

## 内联公式规则

### 规则 1：含多个下划线的内联公式，必须用 `$` + 反引号 语法

当内联公式中出现 **2 个及以上 `_` 下划线**时（包括下标 `_{}`），必须使用 `` $`...`$ `` 语法，防止 Markdown 将两个 `_` 之间的内容误判为斜体。

```markdown
<!-- ❌ 错误：多个下划线会被 Markdown 解析为斜体 -->
$a_{ij}$ 
$\sigma(i) = j$    <!-- 虽然只有一个 _，但 () 和 = 组合也可能出问题 -->
$r_1 < r_2 < \cdots < r_{n-1}$

<!-- ✅ 正确：用反引号包裹 -->
$`a_{ij}`$
$`\sigma(i) = j`$
$`r_1 < r_2 < \cdots < r_{n-1}`$
```

### 规则 2：简单的单下标内联公式，可以用普通 `$...$`

只有**单个下标且结构简单**的公式（如单个变量的简单下标），可以用普通 `$...$`：

```markdown
<!-- ✅ 这些足够简单，普通语法即可 -->
$n$, $i$, $j$, $\sigma$, $\tau$, $n!$, $+1$, $-1$
$A$, $M$, $(n-1)$
```

### 规则 3：判断经验法则

**用 `` $`...`$ ``**（安全模式）的场景：
- 公式含有下标：`a_{ij}`, `M_{ij}`, `S_n`, `r_1`
- 公式含有函数调用 + 下标组合：`\sigma(i)`, `\tau(s)`, `f(x_0)`
- 公式含有多个符号的等式：`\sigma(i) = j`, `a_{ij} = 0`
- 公式含有集合运算（`\setminus`, `\cup`, `\cap`）与下标组合

**用 `$...$`**（普通模式）的场景：
- 单独的字母或数字：`$n$`, `$i$`, `$A$`
- 不含下划线的简单表达式：`$n!$`, `$(n-1)$`, `$+1$`
- 不含下划线的希腊字母：`$\sigma$`, `$\tau$`

**拿不准时，一律用 `` $`...`$ ``，不会出错。**

## 块级公式规则

### 规则 4：块级公式 `$$...$$` 无需特殊处理

GitHub 对 `$$...$$` 块级公式有独立的解析通道，不受 Markdown 语法干扰，可以正常使用所有 LaTeX 语法。

```markdown
$$
A_{ij} = (-1)^{i+j} M_{ij}
$$
```

### 规则 5：`$$` 必须独占一行

```markdown
<!-- ❌ 错误 -->
$$A_{ij} = (-1)^{i+j} M_{ij}$$

<!-- ✅ 正确 -->
$$
A_{ij} = (-1)^{i+j} M_{ij}
$$
```

### 规则 6：`$$` 前后要有空行

```markdown
<!-- ❌ 错误：紧贴上文 -->
因此：
$$
\det(A) = \sum_{j=1}^{n} a_{ij} A_{ij}
$$

<!-- ✅ 正确：前后空行 -->
因此：

$$
\det(A) = \sum_{j=1}^{n} a_{ij} A_{ij}
$$
```

## 不兼容的 LaTeX 命令

以下命令在 GitHub 上**不受支持**，必须替换：

| ❌ 不支持 | ✅ 替代方案 | 说明 |
|-----------|-----------|------|
| `\operatorname{sgn}` | `\text{sgn}` 或 `\mathrm{sgn}` | GitHub 不支持 `\operatorname` |
| `\substack{a \\ b}` | 用逗号平铺：`a, \ b` | GitHub 不支持 `\substack` |
| `\boxed{...}` | 去掉，或改用文字强调 | GitHub 不支持 `\boxed` |
| `\mathbb{R}` | 有时不支持，用 `\R` 测试 | 不稳定 |
| `\bm{x}` | `\mathbf{x}` 或 `\boldsymbol{x}` | GitHub 不支持 `\bm` |
| `\xrightarrow{text}` | `\overset{text}{\rightarrow}` | GitHub 不支持 `\xrightarrow` |
| `\tag{1}` | 不使用公式编号 | GitHub 不支持 `\tag` |

## 集合花括号的写法

内联公式中使用 `\lbrace` 和 `\rbrace` 代替 `\{` 和 `\}`，因为后者的花括号可能被 Markdown 解析器吃掉：

```markdown
<!-- ❌ 内联中可能出问题 -->
$\{1, 2, 3\}$

<!-- ✅ 安全写法 -->
$\lbrace 1, 2, 3 \rbrace$

<!-- ✅ 块级公式中两种都可以 -->
$$
S = \{1, 2, \dots, n\}
$$
```

## 完整示例

以下是一个符合规范的段落示例：

```markdown
对于 $n$ 阶方阵 $`A = (a_{ij})`$，其行列式定义为：

$$
\det(A) = \sum_{\sigma \in S_n} \text{sgn}(\sigma) \prod_{i=1}^{n} a_{i, \sigma(i)}
$$

其中 $`S_n`$ 是集合 $\lbrace 1, 2, \dots, n \rbrace$ 上所有排列的集合。
每个排列 $\sigma$ 都有一个确定的值 $`\sigma(i) = j`$。
余子式 $`M_{ij}`$ 是删去第 $i$ 行第 $j$ 列后的 $(n-1)$ 阶行列式。
```

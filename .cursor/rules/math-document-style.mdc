# 数学文档写作风格规范

本项目用于撰写数学笔记和证明，文档格式为 Markdown + LaTeX 数学公式，发布在 GitHub 上。

## 文档结构

### 标题层级
- `#` 一级标题：文档主题
- `##` 二级标题：大的章节（如"定义"、"定理"、"证明"、"直觉理解"）
- `###` 三级标题：章节内的子板块
- `####` 四级标题：证明中的分步骤（如"第一步"、"第二步"）

### 典型章节顺序
1. 定义 / 基本概念
2. 定理陈述
3. 证明（分步骤，先给思路再展开）
4. 直觉理解 / 几何解释（帮助建立直觉）
5. 推论 / 扩展（相关结论）

### 章节之间用 `---` 分隔线隔开

## 写作风格

### 清晰第一
- 每个步骤只做一件事，避免在一个段落中同时推导多个结论
- 关键等式用块级公式 `$$...$$` 独立展示，不要挤在行内
- 证明的每一步都要说明"为什么这样做"，不要跳步

### 用粗体标注关键概念和子步骤
```markdown
**构造子排列**：对于满足 ...
**符号关系**：将固定映射 ...
**代入求和**：当 ...
```

### 用引用块标注关键问题或注释
```markdown
> **核心问题**：为什么 ...
> 这里用到了 ...
```

### 行文语言
- 使用中文书写，数学术语保持通用叫法
- 适当保留英文术语作为括号注释，如"行列式（determinant）"
- 证明语言严谨但不晦涩，面向有一定基础的读者

## 文件组织

### 文件命名
- 使用中文命名，简洁描述主题
- 示例：`行列式定义与展开定理证明.md`、`特征值与特征向量.md`

### 目录结构
- `docs/` 目录存放所有数学文档
- 如果内容增多，按学科子目录组织：`docs/线性代数/`、`docs/微积分/`、`docs/概率论/`

## 数学公式排版

### 详细的公式兼容性规范见 `github-math-formula.mdc`

### 公式与文字的衔接
- 块级公式前后必须有空行
- 引出公式的文字以冒号结尾
- 公式后如果有解释，另起一段

```markdown
因此：

$$
\det(A) = \sum_{j=1}^{n} a_{ij} A_{ij}
$$

其中右边的求和式就是按第 $i$ 行的展开。
```

### 长公式换行
GitHub 的 `$$` 块支持 `\\` 换行和 `\begin{aligned}` 环境：

```markdown
$$
\begin{aligned}
\det(A) &= \sum_{j=1}^{n} a_{ij} A_{ij} \\
&= \sum_{j=1}^{n} (-1)^{i+j} a_{ij} M_{ij}
\end{aligned}
$$
```
